import { Divider, Grid } from '@arco-design/web-react';

const Row = Grid.Row;
const Col = Grid.Col;

function FooterIndex() {
  return (
    <>
      <Divider />
    </>
  );
}

export default FooterIndex;
